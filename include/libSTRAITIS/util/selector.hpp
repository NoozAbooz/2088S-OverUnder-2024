#pragma once
#include "main.h"

namespace strait
{
    namespace selector
    {
        extern int auton;
        void init();
        void writeIntToFile(int num);
        void readIntFromFile();
    }
}